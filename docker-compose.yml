version: '3'
services:
  app:
    image: node:current-alpine3.16
    volumes: 
      - ./:/app
    working_dir: /app
    ports: 
      - 3000:3000
    command: yarn dev
    tty: true
    networks:
      - pygesres_net
    extra_hosts:
      - 'host.docker.interal:host-gateway'
networks:
    pygesres_net:
        name: pygesres
        external: true
